# quiz-app

一般常識クイズアプリ。HTML/CSS/JavaScriptで構築するシングルページアプリケーション。

## プロジェクト概要

- **アプリ名**: quiz-app
- **種別**: 一般常識クイズアプリ
- **技術スタック**: HTML / CSS / JavaScript（バニラJS）
- **構成**: シングルページアプリケーション（SPA）

## ファイル構成

```
quiz-app/
├── index.html        # メインHTML（単一ページ）
├── style.css         # スタイルシート
├── script.js         # クイズロジック・UI制御
└── questions.js      # クイズ問題データ
```

## 開発ルール

### JavaScript
- バニラJS（ライブラリ・フレームワーク不使用）
- `const` / `let` を使用し `var` は使わない
- DOM操作は `querySelector` / `querySelectorAll` を使用
- イベントリスナーは `addEventListener` を使用

### HTML
- セマンティックなHTMLタグを使用する（`section`, `article`, `header`, `main` など）
- `lang="ja"` を `<html>` タグに必ず設定する
- アクセシビリティを考慮し `alt` 属性や `aria-label` を適切に設定する

### CSS
- クラス名はケバブケース（例: `.quiz-container`, `.answer-btn`）
- レスポンシブデザイン対応（モバイルファースト）
- CSSカスタムプロパティ（変数）でカラーパレットを管理する

## クイズアプリの主要機能

1. **問題表示**: 1問ずつ表示し、進捗（例: 3/10）を示す
2. **選択肢**: 4択ボタンで回答する
3. **正誤フィードバック**: 回答後に正解・不正解を即時表示する
4. **スコア集計**: 全問終了後に正答数と正答率を表示する
5. **リトライ**: もう一度挑戦できるボタンを設ける

## 注意事項

- 外部APIやサーバーサイド処理は使用しない（完全クライアントサイド）
- 問題データはJavaScriptの配列・オブジェクトとして管理する
- コメントは「なぜそのコードが必要か」が自明でない場合のみ記載する
